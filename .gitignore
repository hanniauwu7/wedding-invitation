node_modules
dist

# ─── Admin Panel (local only, not for production) ───
src/admin/
src/templates/
plugins/

# ─── Environment variables (credentials — NEVER push to git) ───
.env
.env.local
.env.*.local